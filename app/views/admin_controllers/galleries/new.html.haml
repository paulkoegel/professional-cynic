%h1 Create a New Gallery
.m-form--row
  %label Name
  %input.m-form--input.m-input{ name: 'gallery_name', type: 'string' }

%h3 Add Images
%input#js-image-chooser{name: 'image-chooser', type: 'file', multiple: 'multiple'}
(Use <i>Shift</i> or <i>Control</i> to select mutiple files)

%h4 Image Previews
.image-previews

.actions
  %button.btn.btn-success#js-upload-new-gallery
    .icon.ok
    Create new Gallery

%h4 SHA-256 Checksums
.sha256-results

%h4 EXIF Data
#exif-tables
